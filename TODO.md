# TODO

Только то, что ещё не закончено. Выполненное удаляется; история — в
Git и `docs/CHANGELOG.md`, устройство систем — в `docs/`.

## Критично

- **Проверка на Android-устройстве.** На железе не проверено ничего
  из сделанного после аудита: всё мерено на хосте и lavapipe.
  - Собрать APK, пройти жизненный цикл: сворачивание и возврат,
    смена аудиоустройства, Vulkan на Mali / Adreno / PowerVR.
  - Кадр p50/p99 в густом лесу на дальности 7 и 10 (растения —
    модели, `render_passes` для разбивки по проходам); метрики ТЗ:
    60 FPS на Snapdragon 660, ОЗУ ≤ 1.5 ГБ, draw calls < 500.
  - Генерация чанка p95/p99 на устройстве.
  - Глазами: растения (стволы в тени отдают лиловым под `mob.frag`),
    ветер, граница дальности мелочи.

- **Боссы пропадают и возвращаются** (`mobs/spawner.cpp`).
  - Деспавн дальше 64 блоков (`Spawner::update`) удаляет и босса,
    хотя комментарий там же обещает обратное; `bossPlaced_` при этом
    не даёт поставить его снова до перезапуска.
  - `bossPlaced_` не сохраняется и обнуляется при каждом возврате
    окна: убитый босс появляется заново.
  - Проверка: уйти и вернуться — босс на месте; убить, сохранить,
    загрузить — босса нет.

- **Хвост генерации чанка вырос**: p95/p99 на 3–27 % выше, чем до
  переделки генератора (чанки с реками и долинами; замеры —
  `docs/WORLD_GENERATION.md` §9а). Найти, что дорожает, и вернуть p99
  к прежнему; мерить `tools/worldmap --stats`.

## Важно

- **HUD на мелком экране**: круглые кнопки «PUT»/«USE» накрывают
  строки золота, воздуха и задания (направление 16,
  `docs/AAA_DIRECTION.md`). Решить, что из этого показывать на
  мелком экране, и снять явное исключение в проверке раскладки HUD
  (`tools/hostcheck/tests.cpp`, «кнопка … накрывает»).

## Обычный долг

- **Жизненный цикл окна**: движок разбирается целиком при каждом
  сворачивании — мир, звук, спавнеры, модели растений строятся
  заново. Держать устройство Vulkan, мир и звук; пересоздавать только
  поверхность и цепочку показа (`docs/ENGINEERING_AUDIT.md` §4).
- **Планировщик**: воркеры в простое просыпаются каждые 2 мс
  (`core/job_system.cpp`) — батарея в меню и на паузе. Убрать опрос
  только вместе с потерей пробуждения при постановке в локальную
  очередь (`notify_one` без общего замка).
- **Слои генерации мира в игре**: отладочный вид с переключаемыми
  слоями есть только на хосте (`tools/worldmap --layer`); на
  устройстве слоёв нет.
- **Мёртвый код**: 38 объявлений без вызовов
  (`tools/hostcheck/find_unused.py`) и `Recipe::craftTime`, который
  задаётся, но не читается. Удалить или подключить.

## Зависимости

- Замеры на устройстве решают, браться ли за бюджет выгрузки чанков,
  приоритет воркеров и пул чанков (`docs/STABILITY_AUDIT.md` §4,
  A3–A5) и насколько срочен хвост генерации.
- Переделка жизненного цикла снимает сброс `bossPlaced_` при
  возврате окна, но не сохранение боссов в сейв — его делать в любом
  случае.
