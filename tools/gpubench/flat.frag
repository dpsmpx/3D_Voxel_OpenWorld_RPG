#version 450
// Опорный проход: та же растеризация и та же запись в цель, но
// фрагментной работы нет вовсе. Разница с настоящим шейдером и есть
// его собственная стоимость.
layout(location = 0) out vec4 outColor;
void main() { outColor = vec4(0.42, 0.62, 0.92, 1.0); }
