/**
 * @file player_rig.h
 * @brief Оснастка игрока: тот же двуногий, что у NPC.
 */
#pragma once
#include "../entity/humanoid_rig.h"

namespace player {

/// Рост игрока, метры. Совпадает с высотой коллайдера (0.90 × 2):
/// модель и физика обязаны быть одного размера, иначе игрок
/// протискивается там, где визуально не пролезает.
constexpr f32 PLAYER_HEIGHT = 1.80f;

/// Оснастка игрока. Строится один раз.
///
/// Той же функцией humanoidRig, что и NPC: игрок — не особый случай,
/// а такой же двуногий. Отличают его пропорции и цвета, а не
/// отдельный код.
const entity::Rig& rig();

} // namespace player
