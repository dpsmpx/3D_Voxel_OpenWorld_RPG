/**
 * @file mob_rigs.h
 * @brief Оснастка мобов: иерархия частей по видам.
 */
#pragma once
#include "rig.h"
#include "../core/types.h"

namespace mobs {

/// Оснастка вида. Строится один раз и живёт до выхода.
///
/// Прежние определения были плоским списком коробок: собрать
/// «торс → бедро → голень» было не на чем. Здесь тот же силуэт
/// получает суставы, и анимация начинает вращать, а не сдвигать.
const entity::Rig& rigFor(u16 mobId);

} // namespace mobs
